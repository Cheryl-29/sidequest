# Sidequest 项目计划 v2

日期：2026-09-14  
上一版：v1.1（2026-09-13）  
状态：M0–M2 已完成并通过验证；本次修订调整产品定位与研究主线，M3 之后的阶段重排。  
主要目标：完成一个有真实数据、有技术深度、可定量评测的 Agent 应用开发求职项目。

**本次修订**：产品从"行程验证器"回到"灵感提供器"。主线任务假定为上班上学，因此不再追求"非日常性"，删除新颖性指标与日常集合采集；排序回到「可行 × 合身」两轴。研究主问题从"有限预算下该继续查什么"改为"不确定用户口味时该探索还是利用"。评测由三层简化为两层。经后续 v2.3 修订，生产口味维度收敛为 D1、D2；D3、D4 仅保留为研究项。已完成的确定性内核与证据体系全部保留，定位从产品卖点降级为地基。

**v2.1 修订（2026-09-14）**：放弃"只在人工核验的静态 allowlist 里挑"。Agent 要能围绕用户位置或所选出发地**实时发现真实地点**；静态候选池降级为 Replay 评测基准与数据源未接入前的临时策略。首版判定范围收窄为**时间预算**（去程 + 停留 + 回程 ≤ 空闲时段）：缺失的营业时间仍为 unknown、绝不算 pass，但改为不参与判定的提示项；已知的关门时间照样判失败。模型拿到出发日期、星期与时间窗口，负责避开大概率不营业的地点（软过滤，不是证据）。营业时间验证之后补回判定。受影响：§1.2、§2.1、§4、§4.2、§4.3、§5、§6.1、§10、§13。

**v2.1 补充（同日）**：支线可以小到「30 分钟去楼下超市买点零食」。新增 9 类日常地点（超市、便利店、面包店、奶茶、冰淇淋、咖啡馆、书店、二手店、花店），每类带一个动作提示交给叙述；停留时长本身就放不进窗口的类别在检索时剔除。选择加入**受控随机**：在合身度相近的前 5 名里按分数加权抽样，种子写入 Trace，评测固定种子。随机放在执行器而非模型温度里。受影响：§2.1、§4.3、§4.4、§5。

**v2.2 修订（2026-09-14）**：**记忆管理升为主亮点。**v2 让口味维度同时承担三个角色——`choose_probe` 的动作空间、persona 的隐藏向量、`TasteState` 的记忆 schema——结果长期记忆只剩三个维度计数器，撑不起"越来越懂你"。本次把第三个角色拆出去：**维度仍然窄而严**（冻结、过闸门，只管试探与评测分层）；**记忆改为类型化条目**（维度倾向、地点类别、具体地点、自由笔记），条目可带**由请求确定性推出的情境条件**（午休 / 下班后 / 周末 × 窗口长短），并由一条可审计的管理流程维护：情节记录 → 规则合并 → 提议与确认 → 冲突裁决 → 例外与漂移 → 召回 → 删除级联。评测新增**跨会话层**：同一 persona 在不同情境下多次回来，主指标是第 N 次会话的首轮接受率，并以"仅维度的扁平 Profile"作为消融对照。受影响：§1.2、§1.3、§2.2、§3.2、§4.2、§4.3、§6、§7.3（新增 §7.4）、§8.2、§8.3、§8.5、§9、§10、§11、§12、§13。

**v2.3 修订（2026-09-15）**：**D3 常规度退出全局口味维度。**“喜欢小众”通常只在某个类别或兴趣范围内成立，不能从一次“太大众了”泛化为对所有冷门地点的长期偏好。D3 不再进入 `choose_probe`、全局排序目标、episode signal、长期记忆或 persona 分层；`Candidate.obviousness` 仅保留为研究字段。“太大众了 / 太冷门了”仍是即时 reroll，下一轮在同类地点中作一次局部排序调整，用完即清，不形成记忆。当前生产维度为 D1、D2；类别与情境条目承担更具体的个性化。

**v2.3 补充（记忆泛化，2026-09-15）**：全局条目新增"跨情境合计"路径——所有情境无反例、合计支持达到阈值、跨 2 个会话、且至少两个情境有 reroll 证据即可提议；只有接受记录不能合并（§7.4）。修复了真实模型下 typed_context 反而低于扁平消融组的问题（开发 persona 首轮接受 27/44 → 34/44，§10 M5′）。

**v2.5 收窄（2026-09-16）**：可用时间上限从 16 小时降到**半天（4 小时）**，去掉「一天」快捷项。一天是完整出行，要安排吃饭、休息与节奏，规划器（≤3 站、固定停留、没有餐饮与休息站）撑不起来——实测 8 小时窗口给出 3 小时行程，剩下 5 小时空着却显示已核实；检索半径在 4 小时时已达 12 km 上限，更长窗口不会多找到地点。「一整天有空」由用户自己挑出发时间，不做时段选择。冻结场景 holdout-10 由 10:00–17:00 改为 13:00–17:00（保留「下午看电影」原意）。受影响：§2.1、§3。

**v2.4 收敛（2026-09-15）**：删除不再服务主线的功能与产物。故障注入（`Request.fault`、开发视图面板与 6 个依赖它的冻结场景）移除，冻结场景由 30 个变为 24 个（开发 16、保留 8），关闭与缺失营业时间的不变式改由单元测试直接构造场馆覆盖；D3 的 Wikidata sitelinks 推导、抓取脚本与人工标签移除，`Candidate.obviousness` 只按 OSM 标签计算，仍服务“太大众了 / 太冷门了”的局部 reroll；M0 探针脚本、TfNSW/DDGS 原始快照、`evals/provider-m0.json`、`/api/weather`、Docker 配置、v1 计划与 `docs/m0-next-run.md`、`docs/m3-candidate-shortlist.md` 一并删除。下文提到这些内容处为历史记录。

**v2.3 补充**：D2 加入 `choose_probe`，词面为 `near` / `farther`。只有窗口超过 120 分钟、且本会话和相关记忆都没有 D2 证据时才允许主动试探；短窗口沿用就近先验。Probe 只改变已通过硬约束候选的排序，不推导或修改 `max_walk_minutes`。

---

## 1. 定位与成功标准

**在你有一小段空闲、又不知道做什么的时候，给你一个你自己想不到、但真的会去的支线任务；不喜欢就换，换着换着它越来越懂你。**

### 1.1 主线假设

主线任务是上班、上学这类社会性义务。**任何不属于主线的时间，本身就已经是支线**——周二下午去公园坐 40 分钟，哪怕那个公园去过五次，支线感依然成立。

这条假设有两个直接推论，是 v2 与 v1 最重要的区别：

1. **不需要寻找"非日常"的候选。** 一个喜欢安静美术馆的人，第四次被推荐安静美术馆没有任何问题，那正是懂他。把"与用户历史的距离"当作排序目标会推着系统去推荐越来越偏的东西，直接与合身度对打。
2. **可以对时间做强假设。** 空闲时间是从工作日或学业里挤出来的碎片，起点大概率是公司、学校或家，时段集中在午休、下班后、周末上午。"午休 1 小时"与"周六下午空出来了"是两种形状完全不同的任务，而这个判断从时间点即可推出，不需要询问用户。

### 1.2 三个承诺

按优先级排列：

| 承诺 | 含义 | 当前状态 |
|---|---|---|
| **合身** | 建议贴合当前处境与用户口味；从反馈中学习 | 未实现，v2 核心 |
| **会学** | "换一个"不是重掷骰子，系统据此收敛；**跨次记得，并且知道在什么情境下记得**（v2.2） | 会话内信念已实现（`taste.py`）；类型化记忆未实现，v2.2 核心 |
| **不是假的** | 去得到、赶得回来；开着门先作提示，后补为判定（§6.1） | 全链条验证已实现（M1/M2）；实时发现路径的判定范围见 §6.1 |

第三项是地基不是门面：没有它，产品就是一个会推荐已倒闭店铺的 LLM 套壳；但它不是用户打开这个网页的理由。

### 1.3 完成标准

1. **产品闭环**：陌生用户输入一句话和一段时间，得到一个有依据的支线任务；点"换一个"并给出理由后，得到一个方向明显不同的新任务。
2. **工程闭环**：真实数据接入、结构化工具契约、持久状态、错误恢复、来源与 Trace、可部署服务均可运行。
3. **评测闭环**：硬约束回归 + 模拟用户收敛实验（会话内）+ **跨会话记忆实验**（v2.2），含对照组、保留集、失败分析和实际数字。

不以用户增长、商业化或主观"很有趣"作为主要验收条件；人工相关性标注作为补充。

---

## 2. 产品范围

### 2.1 首版范围

| 维度 | 决定 | 相对 v1 |
|---|---|---|
| **地域** | **以用户位置或所选出发地为中心实时发现地点**；路线验证依赖 TfNSW，因此限 NSW，`Request` 当前仍限悉尼范围 | **v2.1 改**：原为 M0 收缩后的少量 CBD 核验场馆 |
| **任务尺度** | 从「楼下超市买个零食」到「去海边走一圈」；由空闲时长与地点类别共同决定，停留时长本身放不进窗口的类别不进候选 | **v2.1 补充** |
| **出发时间** | 默认"现在"，截止 = 现在 + 用户说的空闲时长；用户不必填写 | **v2.1 明确**（CLI 已如此） |
| 日期 | 今天或明天 | 不变 |
| 时长 | 30 分钟至半天（4 小时）；计入去程、停留、必要等待与回程 | **v2.5 收窄**：一天是完整出行，需要吃饭、休息与节奏安排，不属于支线 |
| 用户 | 单人使用场景 | 不变 |
| 交通 | 步行与公共交通 | 不变 |
| **输入** | **一句话 + 一段时间**；其余全部由系统推断，可展开调整 | **改**：原为多字段表单 |
| **输出单位** | **一个支线任务**，可 reroll；不并排比价 | **改**：原为 2–3 条可比较行程 |
| 任务内部结构 | 一个主要目的 + 0–2 个顺路停留，共 1–3 站 | 不变（但不再追求塞满） |
| **反馈** | **每次 reroll 必须带类型化理由** | **新增** |
| 预算 | 展示已知门票费用及未知项；证据充分才判断指定预算通过 | 不变 |
| 动态更新 | 生成时查询、修改或刷新时重验；无后台监控推送 | 不变 |
| 交易 | 外链查看、导航或购票；不支付、不预约 | 不变 |

**取消的范围**：新颖性指标、日常集合采集、onboarding 询问日常习惯、三卡并排比较。

1–3 站的链条保留为一个任务**内部**的结构（"先去 A 看个东西，顺路 B 坐一会儿，走回来"）。v1 把"2–3 站"设为验收硬指标导致打分函数最大化站数；v2 取消该硬指标，站数由时间与合身度自然决定。

### 2.2 核心交互

1. **输入**：一句话（"下午空出来了，不想待在家"）+ 时间。提供"现在 / 今晚 / 明天"和"30 分钟 / 1 小时 / 2 小时 / 半天"快捷项（上限 4 小时）；系统显示推断出的返回时间与起点，可修改。
2. **任务卡**：单个支线任务。包含任务简报（去做什么）、为什么是现在、为什么可能适合你、怎么安排（时间线、路线、已知费用）、不确定项。
3. **换一个**：点击时必须选择理由 chip，可附一句话。chip 分三类，作用域不同，**不可混用**：

| 类 | chip | 作用 |
|---|---|---|
| 移动口味维度 | `想坐着` / `想动一动`（D1）、`太远了` / `想走远点`（D2） | 更新 `TasteState`，可能累积为长期口味 |
| 局部换一个 | `太大众了` / `太冷门了` | 只调整下一轮同类地点的顺序，用完即清，不形成长期记忆 |
| 只改硬约束 | `不想花钱`、`时间不合适` | 改写 `Request`，不进 `TasteState` |
| 只进去重集合 | `去过了` | 写入已消费集合，不进 `TasteState` |

第三类尤其重要：**"去过了"不表达任何口味**。把它当作负反馈是错误记忆最典型的来源——用户可能正因为喜欢才去过。维度编号见 §4.3。

**v2.2 增补**：chip 不再只是维度的遥控器，而是记忆的输入。新增三个 chip、一个接受动作和一个自由文本入口（记忆条目类型见 §7.4）：

| chip / 动作 | 作用域 | 作用 |
|---|---|---|
| `不想要这类`（附当前类别） | 移动口味（类别） | 本会话降权该类别；多次出现后可提议写入类别条目 |
| `以后别推这个` | 用户明示（地点） | 用户自己说出的长期意愿，按 `source=user_stated` 经 `confirm()` 写入地点条目，跨会话硬跳过 |
| `喜欢这个地方` | 用户明示（地点） | 写入地点收藏；**允许以后再推**（§1.1 推论 1），不是去重 |
| **`就这个`**（接受） | 情节 | 记为弱正反馈情节；与该任务的维度、类别和情境一起留存，是合并规则的正向证据来源 |
| `其他` + 一句话 | 仅本会话 | 模型尝试映射到已有 reason 或类别；映射不上的只作本会话软上下文，可生成**笔记**提议，不参与打分 |

没有 `就这个`，系统只能从拒绝里学习，永远不知道"对了"是什么样子；接受信号因此是 v2.2 的必需项，不是装饰。

4. **展开**：查看完整行程、来源、验证结果；锁定或删除某一站。
5. **偏好**：系统在检测到稳定信号时**提议**写入长期偏好，由用户确认；用户也可手动查看、编辑、删除。**v2.2**：偏好抽屉按条目展示——每条写明内容、生效情境（如"工作日午休"）、来源（你说的 / 系统提议并经你确认）、支持它的几次经历，并可一键删除；删除后的下一轮不得再受其影响。每会话最多主动提议一次，不打断主流程。

四个组件：输入区、任务卡、任务详情、偏好抽屉。开发视图（Trace、工具调用、淘汰原因、证据状态、耗时）独立抽屉，不挤占主界面；故障注入从主界面移入开发视图。

### 2.3 输出的诚实边界

沿用 v1 §2.3，全部保留：

- 每个任务回答"去做什么、为什么这个时间值得去、为什么可能适合你、这一趟怎么安排"。
- 日期、天气、开放状态等契机必须关联真实证据。**可以创作标题与叙述，不能创作事实。**
- 不知道用户偏好时，不伪造个性化理由。
- 价格范围不等于当前可购买报价；已知最低价在预算内只能说明可能符合。
- 室内目的地不等于全程不淋雨；缺少遮蔽路线证据时只说明减少户外暴露。
- 搜索预算耗尽不等于整个城市无解。
- 停留时长区分官方结束时间、用户指定时长与系统建议；系统建议要标注。
- 演出等不可缩短的体验缺少可靠结束时间时，不能声称已验证完整参加后按时返回。

v2 新增两条，针对"懂你"这个新承诺：

- **不把一次拒绝伪装成长期理解。** 系统说"因为你之前说过想安静"必须对应真实记录；仅凭一次反馈不说"我知道你喜欢……"。
- **不把推断说成用户说过的话。** 从时间点推断出的"你大概在午休"要以推断语气呈现，用户可一键否认。

### 2.4 支线感落在哪里

既然不通过候选筛选制造支线感，它由两处承担：

- **叙述与语气**：任务简报式的文案，"你从主线里偷出来的两小时"这类口吻。属于生成层，不属于排序层。
- **时间先验**：见 §1.1 推论 2。系统对"这段时间是从什么里挤出来的"有判断，并让它影响任务形状（午休 → 单站、近、可预期；周末上午 → 可以走远一点、允许更松散）。

---

## 3. 研究问题与亮点

### 3.1 主问题：不确定用户口味时，该探索还是该利用？

第一次建议时，系统对用户几乎一无所知——只有一句话和一个时间。它可以给最稳妥的大众选择（利用先验），也可以拿这次建议去试探某个维度（室内外？走动还是坐下？安静还是热闹？）。连续两次被拒之后，是继续试探，还是收敛到已知信息上押注？

**H1：在只有一句话和 k 次反馈的条件下，agent 主动降低对用户口味的不确定性，比固定策略更快收敛到被接受的建议。**

对照：固定策略（按先验相关度排序，被拒则取下一名）vs Agent（显式选择试探维度）。共用同一候选池、同一验证器、同一工具预算。若实验不支持，保留固定策略为默认路径，并报告 Agent 收益的适用场景。

这个问题继承了 v1 H1 的形式（有限预算下的决策），但把决策对象从"补查哪个字段"换成"押注哪个口味维度"。补查决策作为子问题保留在 §4.2 动作空间里。

### 3.2 亮点一：反馈驱动的记忆分层

用户偏好可以跨次使用；天气、营业状态、票务与交通必须按来源和有效期重新核实。当前明确要求优先于历史偏好。

信息量最大、摩擦最小的信号是 **reroll 的理由**，不是用户手填的偏好表单。三次 reroll 能得到比任何表单都准的画像，且用户零心智负担。

三层状态：

| 层 | 例子 | 生命周期 |
|---|---|---|
| 会话约束 | "今天不想走远" | 仅本次 |
| 长期口味 | 连续三次拒绝室内 → 提议写入 Profile | 跨会话，需用户确认 |
| 已消费集合 | 推荐过 / 去过的 | 跨会话，用于去重 |

**H2：结构化偏好与相关历史召回，相比完整历史上下文，在保持或改善偏好遵循率的同时减少 Token 和错误记忆。**

"已消费集合"只用于**去重卫生**（最近推过的别再推），不是新颖性追求——这是 v2 与 v1 在这一点上的关键区别。

**v2.2：本节升为主亮点，从"分三层"深化为"类型化记忆 + 管理流程"。**

三层生命周期保留，但"长期口味"不再是三个维度计数器，而是一组类型化条目（§7.4）。真正值得讲的不是存了什么，而是每一步由谁决定、凭什么：

| 操作 | 谁决定 | 规则要点 |
|---|---|---|
| 记录情节 | 代码，自动 | 每次 reroll 与接受都追加一条 `Episode`，带当时情境快照；不需确认，可删除，可"本次不保存" |
| 合并 | 代码 | 同一条目在同一情境下的支持数达到阈值且无反例，才成为**可提议**；模型不能让一条记忆变得可提议 |
| 提议 | 模型选时机与措辞 | 只能从可提议集合里挑；必须附支持的情节 ID；每会话最多一次 |
| 确认 | 用户 | 唯一写入 Profile 的路径；拒绝的提议被记住，支持数翻倍前不再提 |
| 冲突裁决 | 代码，固定优先级 | 本会话反馈（最新的明确表态）> 开场明确说出的要求 > 情境匹配的条目 > 全局条目 > 模型推断的开场意图 |
| 例外与漂移 | 代码判定，用户确认 | 一次反例只算例外，不改 Profile；同情境反例累积到阈值才提议收窄或退役；**不同情境的反例不算反例**，而是情境拆分的证据 |
| 召回 | 代码 | 每轮只召回情境匹配与全局条目，按预算截断；Trace 记录召回了哪些、是否改变了排序 |
| 删除 | 用户 | 删除条目或情节后，派生条目按剩余支持重算；支持不足则回到未确认 |

**H2′（v2.2，取代 H2 成为记忆主假设）：带情境条件的类型化记忆，相比仅维度的扁平 Profile 与完整历史上下文，在跨会话场景下提高首轮接受率，且不提高情境外误用率与错误记忆率。**

H2 的"完整历史 vs 结构化召回"对照保留为 H2′ 的一个对照组。

### 3.3 亮点二（次要）：证据变化后的局部修复

保存结论依赖的证据。某站关闭后替换该站并重算相邻路段；停留变长后将时间变化传播至后续各站与回程；路线变化后重验到场、换乘、步行和回程。

仅复用未受影响且仍新鲜的证据。公共交通路线与出发时刻相关，即使起终点不变，时间改变后也不能沿用原班次。

**H3：相较从头规划，局部修复减少工具调用与恢复延迟，且不增加错误通过率。**

**降为次要**：v1 把局部修复作为并列亮点，但 v2 的核心循环是"换一个任务"而非"修复同一个行程"。局部修复在证据变化（场馆关闭、路线延误）时仍有价值，且基础设施（路线缓存、故障注入）已经就位，因此保留但不作为主线投入。

用户显式锁定的站点必须保留；无法满足时说明冲突，不擅自替换。

---

## 4. 架构与职责

```text
React + TypeScript
        ↓ 请求 / SSE 进度 / reroll + 理由
FastAPI：任务创建、反馈、修改、取消、结果读取
        ↓
Agent 循环：意图推断 → 口味状态 → 选题决策 → 补查决策 → 叙述生成
                              ↑                ↓
                              └─ reroll 反馈 ──┘
        ↓
执行器（确定性，禁止 LLM 介入）：
  候选检索 / 组合与顺序 / 全链条验证 / 时间与预算算术 / 证据有效期
        ↓
工具适配层 / 状态与记忆 / 缓存 / Trace / Replay
        ↓
TfNSW / OSM 本地地点索引（§5）/ 天气；静态场馆 allowlist 仅用于 Replay 与临时 Live
```

**v2.1 的一轮流程**（已实现；第 2、3 步在 `Request.catalog="osm"` 时启用，第 4 步之后加受控随机抽样，§4.4）：

1. `infer_intent`：一句话 + 出发日期、星期、时间窗口 → 结构化意图。
2. **生成搜索意图**（`search_places`）：模型根据意图与口味信念，从固定的 18 个地点类别里选要找哪几类，并被要求避开这个时段大概率不营业的地方。
3. **执行器落地**：用地点数据源在出发点周边检索，得到真实存在的地点；模型凭常识提议的地名也必须经此落地才能被采用。
4. 模型从检索结果中押一个（`locked_ids`，沿用"锁定站点不被静默替换"）。
5. `plan()` / `validate()`：TfNSW 查往返路线，判定时间预算（§6.1）。
6. `narrate()`：未核实项不得写成事实。

**LLM 决定找什么、押哪个；执行器决定它存不存在、去不去得了。**这条边界不因候选从静态池变为实时检索而改变。

### 4.1 分层原则

**LLM 负责判断与构思，执行器负责计算与验证。** 这条边界决定项目可信度，不可模糊：

| Agent 决定 | 执行器保证 |
|---|---|
| 从一句话推断可探索半径、能量水平、社交度、室内外倾向 | Schema 校验；推断结果必须是 typed intent |
| 这次押注哪个口味维度（探索 vs 利用） | 候选必须来自检索结果，不接受编造地点 |
| 选中候选后补查什么字段 / 换候选 / 降级为 conditional | 调用预算、超时、来源白名单 |
| 撰写任务简报与"为什么是现在" | 事实必须引用证据 ID；无证据的陈述拒绝渲染 |
| 解读拒绝理由，决定是否提议写入长期偏好 | 记忆写入需用户确认；作用域由代码区分 |
| — | 时间计算、预算算术、全链条验证、verified/conditional/infeasible 判定 |

**LLM 永远不产出时间、费用和可行性结论。** 这三样已经由 `planner.validate()` 实现并有 43 项测试覆盖。

### 4.2 动作与权限

沿用 v1 §4.1 表格，按 v2 增补：

| Agent 可决定的动作 | 执行器的限制 |
|---|---|
| `infer_intent`：从一句话与时间推断结构化意图 | 输出 typed schema；推断项标记为推断，用户可否认 |
| `choose_probe`：选择本次试探的口味维度 | 必须从固定维度集合中选择；记录不确定性依据 |
| `search_places`（v2.1 新增）：给出搜索意图 | 模型只产出结构化查询词与类别；中心点、半径、端点由执行器决定，用户与模型文本不拼接 URL；结果必须来自数据源 |
| `retrieve_candidates`：召回匹配候选 | 硬过滤不由模型绕过；保留未知候选状态；已知整段关门的候选在规划前剔除 |
| `inspect_candidate`：补查详情或来源 | 仅允许已发现的候选与来源；网络访问只经数据源适配器，不允许任意 URL |
| `check_weather` / `plan_route` | 时间范围与坐标校验；返回原始获取时间 |
| `propose_quest`：提交结构化任务草案 | 目的地必须来自候选；不接受模型编造路线时间 |
| `interpret_feedback`：解读 reroll 理由 | 作用域（本次 / 长期）由代码区分，不由模型自称 |
| `propose_memory`：提议写入长期偏好 | 必须用户确认；必须附带支持该提议的历史记录 ID；**v2.2**：只能从代码算出的可提议集合中选，条目类型、键与情境不由模型自称 |
| `recall_memory`（v2.2）：读取与本轮相关的记忆 | 执行器按情境与预算选条目，模型只读不写；笔记类条目只进提示，不进打分 |
| `repair_plan` / `ask_user` / `finish` | 不允许静默放宽硬约束；最终状态由证据门槛决定 |

所有动作输出 action、typed arguments 和简短决策摘要；不要求或保存模型隐藏思维过程。

### 4.3 口味维度集合（已冻结）

> **v2.2 注**：维度不再是记忆 schema。它只保留前两个角色——`choose_probe` 的动作空间与 persona 的分层轴——冻结与闸门规则不变。长期记忆改为 §7.4 的类型化条目，其中"维度条目"只是一种。类别、地点、笔记条目**不需要过闸门**：类别取自固定的 18 类枚举、地点取自候选 id，二者在候选池里天然存在；笔记不参与打分，也就不需要方差。扩展记忆类型因此不会反过来松动维度集合。

维度集合同时承担三个角色（v2.2 起为前两个）：agent `choose_probe` 的动作空间、persona 的隐藏向量、`TasteState` 的记忆 schema。因此每个维度必须**能被候选池区分**、**能被某个 reroll 理由移动**、**与其他维度近似正交**、**不与硬约束重复**、**不涉及敏感属性**。

本次修订初稿提出六个维度。对 13 个候选（8 个 Replay fixture + 5 个 Live 核验场馆）实测后，六个里只有一个直接成立：

| 初稿维度 | 实测结果 | 结论 |
|---|---|---|
| 室内外 | 7 室内 / 6 户外，方差充分 | **保留** |
| 能量水平 | 「走动 == 非室内」在 12/13 上成立 | **并入室内外**：在现有候选里是同一个维度，probe 其一等于白送其二 |
| 安静/热闹 | 仅 4/13 有标注 | **预留**，全量标注后启用 |
| 社交度 | 0/13 有标注，且产品为单人场景 | **删除** |
| 花费意愿 | 11/13 免费、1 个未知、1 个 18 元 | **删除**：零方差，且 `budget_aud` 已是硬约束 |
| 可接受距离 | 非候选属性，由起点与候选几何计算 | **保留**，改名为移动意愿 |

当前结果：**2 个启用维度 + 2 个预留/研究维度。**

| # | 维度 | 两端 | 数据来源 | 状态 |
|---|---|---|---|---|
| **D1** | 形态 | 室内静态 ↔ 户外走动 | 现有 tag（`室内` / `户外` / `散步`） | 启用 |
| **D2** | 移动意愿 | 少折腾（路上时间占比低）↔ 愿意走远 | 由起点与候选几何计算，无需标注 | **OSM 检索池启用；静态 CBD 池仅报告 pending** |
| **D3** | 常规度 | 大众答案 ↔ 冷门 | `Candidate.obviousness`（0–1）；仅用于研究和一次性、同类别 reroll | **预留：不作为全局口味** |
| D4 | 氛围 | 安静 ↔ 热闹 | 需全量标注（当前 4/13） | 预留 |

**D3 仍可描述候选自身，但不再描述用户。** Australian Museum 与 The Mint 的知名度不同，这个地点属性可以帮助一次“换个没那么大众的同类地点”；但用户喜欢某类小众内容，不等于他偏好所有冷门地点。因此 D3 不跨类别、不累计、不进入长期画像。

**扩展规则**：新增维度前必须先证明它在候选池上有方差，且与每个已启用维度的取值一致率低于 80%（反相关与正相关同样算退化）。该检查由 `scripts/check_dimensions.py` 执行，已纳入 CI，产物写入 `reports/latest-dimensions.json`。

### 4.3.1 闸门运行结果与后续修订

> **v2.1 注**：本节"出路 1"（逐个人工核验 19 个候选进 allowlist）不再是主路径。D2 的地理跨度改由实时检索提供；闸门仍须在检索结果的快照上重新跑过，不能默认通过。下文作为静态池阶段的记录保留，shortlist 仍可作为 Replay 评测基准。

闸门首次运行即判定 **D2 在当前候选池上不成立**：13 个候选距三个固定起点的平均距离全部落在 0.99–2.14 km，只有 Barangaroo Reserve 一个越过 1.5 km 阈值，少数侧仅占 8%（门槛 20%）。

**这是数据问题，不是阈值问题。** 任何阈值切出来的两侧差别都在几百米量级，用户不会因为这种差别说出"太远了"。根因是 M0 把首版范围冻结为悉尼 CBD 少量场馆（§5），地理跨度被一并压没了。

**历史结果**：D3 曾在 13 个静态候选上通过；扩大到 OSM 池后少数侧降至 10.9%，未过 20% 闸门。v2.3 不再尝试靠阈值或新数据源把它救成全局维度，改为研究字段。

当前生产维度为 **D1 形态**与 **D2 移动意愿**。Persona 不再要求凑足三个连续维度，改用 D1、D2、类别好恶与情境条件构造可辨别的行为。

**出路 1（扩展候选池到 CBD 以外）已用 M0 探针数据验证可行**，无需新的网络请求：

| 候选池 | n | 距离范围 | 少数侧 | 闸门 |
|---|---:|---|---:|---|
| 现有 | 13 | 0.99–2.14 km | 8% | FAIL |
| 现有 + 探针全部命名对象 | 170 | 0.99–3.54 km | 20% | 勉强通过 |
| **现有 + 探针中带营业时间的** | **33** | **0.99–3.48 km** | **39%** | **PASS** |

第三行是现实路径：157 个命名对象里有 20 个带 `opening_hours`（13%），其中 17 个有官网，官方页面核验有抓手。名单曾见 `docs/m3-candidate-shortlist.md`（v2.4 删除）。**只取这 20 个就足以让 D2 通过闸门且有余量**，不需要为了凑方差扩大到一两百个候选。

三点须记住：

- OSM 的 `opening_hours` 标签是众包数据，**不满足 M0 的证据规则**。这 20 行仍需逐个官方页面核验才能进入 Live allowlist，与现有五个场馆的流程相同。
- 名单里的公园多数缺可核实的开放与费用语义，按 M0 决定只能作 conditional 候选。
- 名单顺带为 **D3 提供了真实方差**：Museum of Contemporary Art 是游客标准答案，Susannah Place Museum、Tin Sheds Gallery、Chrissie Cotter Gallery 不是。D3 的标注工作因此有了可区分的对象。
- 费用方差仍然不足以复活被删除的花费意愿维度：20 个里只有 MCA 一个收费（约 5%），低于 20% 门槛。

出路 2（把 D2 改为「路上时间 / 停留时间」比值）不再需要，作废。

名单里的 19 个候选也已完成 D3 标注（见 shortlist 的 `D3` 列）——常规度是对地点本身的判断，不像营业时间那样需要官方页面核验，因此可以先标好，随核验一起并入。**注意名单 18/19 落在冷门侧**，并入后 D3 的少数侧从「冷门 38%」翻转为「大众 28%」，仍在门槛之上但余量收窄：以后再扩池不能只加冷门场馆，否则 D3 会从另一侧失效。

### 4.3.2 OSM 大池子复测（2026-09-14，v2.2 补记）

在未抓完的全量快照（17 类中 14 类，9844 个地点；从三个固定起点检索得 371 个候选）上复测：

| 维度 | 结果 | 处理 |
|---|---|---|
| D1 | 覆盖 100%，少数侧 32% | 保持启用 |
| **D2** | 覆盖 100%，少数侧 40% | **在 OSM 检索池上启用**；静态 CBD 池仍只报告不卡 CI（地理跨度本来就由检索提供）。闸门脚本的维度状态改为按池区分 |
| **D3** | 当前快照 211 个适用地点中少数侧 **10.9%，不过闸** | **预留研究；不参与生产口味与 persona** |

**D3 失败的原因是推导，不是阈值。**OSM 标签里几乎没有"名气"信号：值得专程去的地点中只有 2% 带 `wikipedia`、16% 带 `wikidata`，`tourism=attraction` 与多语言名称几乎没有。现行公式与人工标注（`evals/d3-labels.json`，31 条，单人标注）在可匹配的地点上 6/9 一致，全部分歧都是"有一篇英文维基就被判大众"的遗产类场馆（Museum of Sydney、Justice & Police Museum 等）。人工标注里大众答案本身也只占约 26%（8/31），靠调标签权重凑到 20% 等于刷闸门，不做。

**v2.3 决定**（v2.4 起 Wikidata 推导与人工标签已删除）：不再把“复测通过并启用 D3”列为里程碑，也不接入 Google Places 热度来维持这个全局维度。

**同时修掉的缺口**：`agent.targets` 此前只对 D1、D3 设目标，"太远了"只会排除当前地点、下一轮并不会往近处押。现在 D2 由本会话反馈与记忆设定（不从开场白猜测，§7.3）。

### 4.4 排序函数

v1 的 `站数 × 30 + 命中标签 × 18 − 总时长 × 0.05`（`planner.py:357`）作废。v2 形态：

```
可行性       硬闸门，不可行直接淘汰（已实现）
合身度       唯一质量轴：当前处境 + 已知口味 + 试探价值
会话内差异度  与本次已拒绝项的类别 / 地理 / 能量距离；低于阈值则跳过
跨会话去重    最近 N 次推荐过的降权（卫生，非新颖性追求）
```

**受控随机（v2.1，已实现）**：排序后，前 5 名中与第一名分差严格小于 3.0（一个维度命中的分值）的候选按「分数 − 下限」加权、无放回抽样，其余保持原序。因此随机永远不会拿一个维度命中去换多样性，也不看用户历史，不是新颖性目标。种子默认随机，写入 Trace 与 `AgentResult.seed`；评测与复现传固定种子。不依赖模型 temperature：LLM 的采样在固定选项上偏向同一批典型答案、无法复现，且会波及意图推断这类需要稳定的步骤。

**跨会话不追新颖，会话内必须真的换。** 若只有合身度一个轴，系统会每次给最稳妥正中靶心的那个，reroll 就失去意义。

权重提为模块级常量，可被 eval 扫描调参，不散落在表达式里。

**v2.2：合身度的输入扩展为召回的记忆条目。**维度条目沿用现有目标向量；类别条目给该类别加减分；地点 `never` 条目在检索后硬跳过（与 `excluded_ids` 同一机制），地点收藏条目小幅加分；笔记条目不进打分。所有记忆加分合计不得超过一个维度命中（3.0），否则一条类别偏好就能压过当前请求明确表达的维度——这与 §3.2 的冲突优先级矛盾。每个被召回并改变了排名的条目 id 写入 Trace，叙述"因为你之前说过"只能引用这些 id。

### 4.5 预算参数

沿用 v1 §4.2，待真实轨迹校准：

- 每次最多 6 次模型决策，生成叙述单独计数。
- 最多 20 次外部请求尝试；缓存命中单独记录，不消耗额度。
- 单工具截止 8 秒；整体服务端截止 60 秒。
- 总 Token 硬上限；第一批真实轨迹后冻结评测配置。
- 超预算返回已有结果与未验证项，不无限重试。

**已知需修正**：`trials >= 6` 的组合上限与 20 次请求预算是两套互不知情的限制，Live 模式下谁先触发取决于缓存命中率。应统一为"按剩余请求预算决定还能验几个组合"，并在 Trace 中记录哪个限制先触发。

---

## 5. 数据

M0 已于 2026-09-14 通过，结论已冻结，详见 `docs/data-feasibility.md`。

**M0 冻结的 Live 范围（v2.1 起为临时策略）**：悉尼 CBD 少量经官方页面核验的场馆（Museum of Sydney、The Mint、Australian Museum、Royal Botanic Garden、Barangaroo Reserve）；路线走 TfNSW Trip Planner；天气走 Open-Meteo，仅展示不参与验证。

**v2.1 目标**：地点由实时数据源围绕出发点检索，实现 `PlannerTools.retrieve`，不从 planner 或 agent 直接调用。当时比较过的数据源如下（已选 OSM，见下文）：

| 数据源 | 优点 | 代价 |
|---|---|---|
| Google Places | 营业时间、简介、热度较全 | 收费；条款限制缓存，影响评测快照 |
| Foursquare Places | 类似的商业目录 | 收费；覆盖需实测 |
| OSM / Overpass | 免费、可存快照 | M0 探针中仅 13% 命名对象带营业时间；公共实例无 SLA |

**已定（2026-09-14）：OSM，成本为零。** 获取方式为批处理快照 + 本地索引：`scripts/fetch_places.py` 向公共 Overpass 发一次固定查询（9 类地点 × 悉尼 `Request` 坐标范围，`out center tags`，不重试），`scripts/build_places.py` 生成 SQLite 索引（`data/places/`，git 忽略，ODbL 署名）。运行时 `places.discover()` 以出发点为中心、按空闲时长推半径（约窗口 1/3 × 0.15 km/分钟，1–12 km）检索，每类每半环最多 4 个，保证 D2 两端都有。Geofabrik `.osm.pbf` 需要新的原生依赖与全澳几何计算，暂不采用；索引构建只依赖 Overpass 形状的元素，换来源时只换获取脚本。

**类别（18 类）**：值得专程去的 9 类——博物馆、美术馆、艺术中心、图书馆、观景点、海滩、自然保护区、花园、公园；日常小事 9 类——超市、便利店、面包店、奶茶店（`amenity=cafe` + `cuisine=bubble_tea`，优先于咖啡馆）、冰淇淋店、咖啡馆、书店、二手店、花店。日常类别带动作提示（如「买一样没吃过的零食」），叙述可据此写一个不涉及事实的小挑战，但不得声称店里有什么商品、口味或价格。餐厅、酒吧暂不收：过于依赖营业时间与预算。停留时长 ≥ 空闲时长的类别在检索时剔除（确定的算术冲突，不是猜测）。抓取按 17 个基础标签逐个查询（奶茶店在咖啡馆查询中返回，建索引时区分）。

**已知风险**：日常类别不参与 D3，会拉低 OSM 候选池上 D3 的覆盖率，可能跌破闸门的 80% 覆盖门槛；需在全量快照上实测后决定是按类别分层计算覆盖率，还是限制日常类别占比。

OSM 字段的用法：名称、坐标、类别直接使用；`opening_hours` 只做**软排除**（完全解析且整段窗口关门才剔除；含 PH、日出日落、跨夜等无法解析的规则一律保留），从不写入 `open_at`/`close_at`；`fee` 不写入 `cost`；D3 由地点自身标签推出（Wikipedia +0.4、Wikidata +0.1、`tourism=attraction` +0.3，基线 0.2）。

实时候选的最低要求：真实坐标、名称、类别、来源 URL 与获取时间；营业时间、费用、简介缺失时逐字段标 unknown，不补默认值。停留时长是规划假设，按类别给默认值（如博物馆 60、公园 45、咖啡馆 30 分钟），标注为系统建议。

**评测可复现**：每次实时检索的响应须保存为快照，供 Replay 与 §8.2 模拟用户评测复用；与数据源缓存条款冲突时，保存可合法保存的最小字段（如 place id + 我方派生属性），并在报告中说明。

**明确不支持**：定时活动仍不进 Live（DDGS/Bing 日期召回错误率过高）；缺 Opal 票价时不因此把方案标为 verified。

**v2 的数据影响**：产品从"比较三条行程"变为"给一个任务"，对候选**数量**的需求下降，对候选**描述质量**的需求上升——任务简报需要能写出"为什么这个地方值得去"的具体细节。当前 5 个场馆的 `description` 字段是手写的，扩展候选池时描述质量是瓶颈，不是坐标或营业时间。

---

## 6. 数据模型与验证

已实现部分（`backend/sidequest/models.py`）：`Request`、`Candidate`、`Evidence`、`Leg`、`Stop`、`Check`、`Itinerary`、`Trace`、`Result`。全链条验证（`planner.validate()`）覆盖：连接连续性、开放窗口、活动到场缓冲与完整时长、最短停留、返回截止、站数、锁定与排除、总步行、已知/未知费用、证据有效性。

状态语义不变：

- `verified`：声明范围内所有硬约束均有有效证据通过。
- `conditional`：没有已知失败，但存在必要未知项。
- `infeasible`：存在明确冲突。

单站分别可行但串联不可行，整趟仍为 infeasible。组合与预算耗尽返回 search_exhausted，不声称穷尽所有城市组合。

### 6.1 判定范围：`Request.venue_facts`（v2.1，已实现）

| 取值 | 缺营业时间 / 场馆证据不覆盖停留时段 | 已知营业时间与停留冲突 | 使用者 |
|---|---|---|---|
| `verdict`（默认） | `unknown`，整趟降为 conditional | fail → infeasible | API、30 场景 eval、全部既有测试 |
| `advisory` | 仍为 `unknown`（**绝不 pass**），`Check.advisory=True`，进入 `Itinerary.advisories`，不参与判定 | fail → infeasible | Agent 路径（`scripts/run_agent.py` 默认；`--strict-hours` 切回） |

`advisory` 下判定只由时间预算（去程、停留、回程、截止时间）和其他已知冲突决定，因此 `verified` 的含义是"时间上赶得回来"，界面必须同时展示"营业时间未核实，出发前确认一下"。锁定、步行、预算、定时活动等检查不受影响。

三层防线，由弱到强：

1. **模型软过滤**：`infer_intent`（以及接入后的搜索意图生成）拿到完整日期、星期和时间窗口，避开大概率不营业的地点。这是提示，不是证据。
2. **已知营业时间预过滤**：数据源明确给出整段窗口都关门的候选，规划前剔除（`agent.shut_all_window`，已实现）。
3. **补丁（待做）**：数据源营业时间稳定后，按来源等级（官方页面 > 商业地点数据源 > OSM 众包；模型常识永不算证据）决定哪些可回到 `verdict`。补丁只让结果更严，不改动本节的数据契约。

叙述约束：`narrate()` 收到 `advisories`，不得声称未核实的地点"现在开着""正在营业"。

**hook 素材（v2.2 补充，已实现）。**hook 要回答"为什么是现在、为什么是你"。百科简介两样都答不了：它写给外地人，与"读者不是游客"冲突；有词条的多是大众答案，会把叙述拉向 D3 的一端；而且内容不随时间变化。因此 `narrate()` 另收一组"此刻"事实（`moment.py`），全部由请求与已验证行程确定性推出、各带可引用的 id，引用校验与站点证据共用一套：

| 素材 | id | 内容与约束 |
|---|---|---|
| 日落 | `clock:sunset:<日期>` | 按日期与出发点坐标计算（NOAA 近似，误差约 ±2 分钟），并给出与行程的关系：出发时已天黑 / 回来前不会天黑 / 那时正在某站 / 那时在路上。只在日落落在行程前后 1 小时内时提供，否则不给这一条（午休时"回来前不会天黑"被模型写成"正好在日落前回到"）。只说几点、人在哪，不说晚霞与景色——那是天气，没有证据 |
| 时间窗口 | `clock:window` | 情境标签（如"工作日午休 · 一个半小时以内"）、空闲分钟、整趟用时、返回时刻与剩余分钟 |
| 路线 | 腿的 `evidence.id` | 每段起止名称、出发时刻、步行或公共交通、步行分钟与换乘次数 |
| 记忆 | `MemoryItem.id` | 只取 §4.4 中实际参与本轮排名、且与所选地点**同向**的条目：维度条目须与地点所在一极一致，类别条目只取 like，地点条目只取 favorite；把地点往下压的条目和笔记不成为"去的理由" |

天气（Open-Meteo，M0 已探）是同类素材的下一项，接入后才能放开"不提天气"的限制。地点"这段时间在办什么"需要检索文本，是 RAG 的切入点，见 §7.1。

**v2 新增模型**：

| 模型 | 关键内容 |
|---|---|
| `Intent` | 从一句话与时间推断的结构化意图：半径、能量水平、社交度、室内外倾向、时间来源（午休/下班后/周末）；每项带 `inferred` 标记 |
| `Feedback` | reroll 的类型化理由、自由文本、被拒任务 ID、候选 ID、时间。**已实现**（`taste.py`） |
| `TasteState` | 会话信念、长期口味（带来源 Feedback ID）、已消费集合、已拒绝的提议；只为 D1、D2 保存观测，D3 仅有一次性局部提示。**已实现** |
| `Quest` | 包住 `Itinerary`，加任务简报、hook、证据引用、本次试探的维度编号 |
| `Candidate.obviousness` | 新增字段：候选的常规度（D3）。候选固有属性，与用户历史无关 |
| `Context`（v2.2） | 由 `Request` 确定性推出的情境桶：`slot`（午休 / 下班后 / 周末白天 / 其他）× `window`（短 / 中 / 长）。封闭枚举，模型不能新增 |
| `Episode`（v2.2） | 一次 reroll 或接受：`Feedback` 或接受事件 + 被展示任务的维度、类别、地点 id + `Context` 快照。只追加，可删除 |
| `MemoryItem`（v2.2） | 长期条目：`kind`、`key`、`polarity`、`context`（空 = 全局）、`status`（proposed / active / retired）、`source`（user_stated / agent_proposed）、支持与反例情节 id、确认时间。详见 §7.4 |
| `TasteState` 变更（v2.2） | `session` 维度信念与 `rejected` 不变；`profile` 由 `dict[Dimension, Belief]` 改为 `list[MemoryItem]`；`declined` 改为按条目键记录 |

统一使用 Australia/Sydney 时区，覆盖夏令时、跨日和缺时区来源（已实现并有测试）。

---

## 7. Context 与 Memory

### 7.1 检索

候选描述与网页相关段落入索引，带 candidate_id、来源、日期、字段类型与更新时间。结构化时间、坐标、费用独立存储。

流程：确定性过滤明确冲突 → BM25 与向量召回 → RRF 融合 → 可选重排序 → 返回候选及证据。必要字段未知的候选标记待补查，不一律当作失败。

**当前状态**：`planner.relevance()` 用的是 `tag in preference` 子串匹配，"想去安静的地方走走"匹配不上 `散步`，且 `matched[0]` 可能是英文 tag 被直接拼进中文文案。至少需要同义词表 + 词级匹配；BM25/向量对照按计划后置。

只完成向量排序时称为检索模块，不把未实现的生成链路算作 RAG。

**RAG 切入点：地点当期内容（v2.2 补充，未实现）。**目前项目没有 RAG：地点检索是结构化的半径查询，叙述收到的是结构化证据与 §6.1 的"此刻"事实。唯一真正需要检索文本、又能回答"为什么是现在"的素材，是地点在这段时间里办的事——"这周换了新展""周四晚上延长开放"。设想：

- **来源**：OSM 索引已保留的 `website` 标签指向的场馆官网展览 / 活动页，批处理抓取（与 `fetch_places.py` 同样手动运行、不进测试），不接受用户输入或模型生成的 URL。
- **切片与证据**：每个片段存 `candidate_id`、`source_url`、`fetched_at`，以及从页面解析出的 `valid_from` / `valid_until`；解析不出有效期的片段不入库。窗口不被覆盖的片段不召回，与现有"证据未覆盖即 unknown"一致。
- **召回**：先按地点 id 与时间窗口做结构化过滤，再在剩余片段内按意图关键词排序（BM25 起步，向量对照后置）。条数少，不需要向量库。
- **生成**：片段以 id 进入 `narrate()` 材料，沿用现有引用校验。片段只供叙述，**永不成为 `open_at`、`cost` 或活动时间等判定事实**——与 OSM `opening_hours` 不进判定同一条规则。
- **范围**：只对博物馆、美术馆、艺术中心、图书馆有意义；定时活动仍按 §5 不进 Live，因此首批只做"展览在展期内"这类无需到场时刻的内容。
- **评测**：补充指标看 hook 中带有效片段引用的比例与人工判定的"具体到现在"比例（§8.4）；主排序与收敛评测不受影响。

不做：用维基百科或 Wikivoyage 简介写 hook（理由见 §6.1 hook 素材）；把用户笔记做向量召回（§7.3，除非 H2′ 对照证明需要）。

### 7.2 上下文

规划上下文只包含当前明确约束、推断意图、当前 TasteState、候选摘要、必要证据和结构化失败摘要。原始网页与完整工具响应存外部存储，不累积到 Prompt。

更新规则：新证据替换过期事实；保留变化原因；被淘汰候选压缩为 ID 与失败原因；每次记录注入字段、Token 量和版本。

### 7.3 记忆必须实现的范围

- **Profile**：长期口味。可由用户明确添加，也可由 agent 从反馈提议后经用户确认写入；两种来源必须区分标记。
- **Episode**：去过、拒绝、喜欢或不喜欢的反馈及当时情境；每条关联触发它的 `Feedback`。
- "今天不想去"和"以后别推荐"作用域不同。
- 当前明确要求覆盖旧偏好；一次例外不自动永久改写 Profile。
- **"太远"不能无依据推断成精确的最大分钟数**；保存定性偏好，必要时澄清。
- 相关历史先按结构化条件与明确反馈召回；向量记忆由对照实验决定。
- 提供查看、编辑、删除与本次不保存；删除同步清理派生索引。
- 演示使用隔离会话，不跨用户召回，不保存精确住宅地址或敏感推断。

### 7.4 类型化记忆模型（v2.2）

**四类条目。**类型决定它能影响什么，这是防错误记忆的第一道闸：

| kind | key 取值 | 例子 | 影响排序 | 硬过滤 | 来源 |
|---|---|---|---|---|---|
| `dimension` | D1 或 D2 的某一端 | 偏户外走动 | 是 | 否 | 反馈合并 → 提议 → 确认 |
| `category` | 18 类枚举之一 | 不想要博物馆 | 是 | 否 | `不想要这类` / 接受情节合并 → 提议 → 确认 |
| `place` | 候选 id，`never` 或 `favorite` | 以后别推这家 | `favorite` 小幅加分 | `never` 硬跳过 | 用户明示 chip，直接以 `user_stated` 确认 |
| `note` | 自由文本（≤60 字） | 不喜欢要排队拍照的地方 | **否** | **否** | `其他` 文本 → 模型提炼 → 提议 → 确认 |

`note` 是唯一不可验证的类型，因此只进 `search_places` 与 `narrate` 的提示，永远不成为分数或过滤条件；它的质量只能用补充指标（§8.4）评价。时间不足时第一个砍掉。

**情境条件。**条目的 `context` 取自 `Context` 封闭枚举，由 `Request` 的出发时刻、星期与窗口长度计算，不询问用户、不由模型声明：

- 新条目**默认带着产生它的情境**，而不是默认全局。"午休时两次嫌远"只说明午休时不想远。
- 同一 `(kind, key, polarity)` 在**两个以上不同情境**都有支持、且**每个**出现过该 key 的情境都无反例时，才提议泛化为全局条目。"有支持"满足以下任一即可（v2.3 补充）：
  - 至少两个情境**各自**达到下表的提议阈值；
  - 或者**合计**支持 ≥ `PROPOSE_SUPPORT`、跨至少 2 个不同会话，且其中**至少两个情境里有 reroll 证据**。方向一致的证据分散在多个情境里，本身就是全局口味最强的信号；原先只认"各情境单独达标"，6 次会话轮换 3 种情境时，全局条目要到第 5 次会话才出现。
  - 只有接受、没有 reroll 的支持**不能合并**：短窗口默认就近排序，用户接受近处地点主要是在回应排序器自己的默认值；允许合并时，不在乎距离的 persona 会被提议全局"就近"并拒绝。代价是只靠接受体现的真实偏好（从不说"太远了"的就近型用户）学不到全局条目——这类偏好本来就由默认值覆盖，首轮行为不受影响。
- 同一 key 在不同情境下极性相反（午休嫌远、周末想走远）时，保留为两条情境条目，**这不是冲突，是本模型要捕捉的东西**。
- 天气情境预留：Open-Meteo 目前只展示、不进 agent 路径，接入前不作为情境维度。

**合并与提议阈值**（模块级常量，初值待 §8.2 校准）：

| 常量 | 初值 | 含义 |
|---|---:|---|
| `PROPOSE_SUPPORT` | 3 | 同情境支持情节数，且其中至少 2 个不同会话 |
| `PROPOSE_MAX_CONTRA` | 0 | 同情境反例数上限 |
| `REVISE_CONTRA` | 2 | 已确认条目在同情境下自上次确认后累积的反例，达到后提议收窄或退役 |
| `REPROPOSE_FACTOR` | 2 | 被拒提议的支持数翻倍前不再提 |
| `STALE_SESSIONS` | 10 | 连续这么多个匹配情境的会话无支持，条目标记 stale，打分权重减半（不删除） |

"至少 2 个不同会话"是关键：同一会话里连续三次嫌远，可能只是今天累了，不足以成为长期口味。会话内信念（现有 `TasteState.session`）照常立即生效。

**正反证据的定义**，写死在代码里，避免"凡是被拒都算负反馈"：

- `dimension`：对应维度 chip 是支持或反例；接受一个在该维度有取值的任务，是对该端的弱支持（计 0.5）。
- `category`：`不想要这类` 是负向支持；接受该类别任务是正向支持，同时是负向条目的反例。
- 不带理由的拒绝、`去过了`、`时间不合适`、`不想花钱` **不是任何条目的证据**（沿用 §2.2 作用域）。
- `太远了` 仍是定性的：只能成为 D2 条目，永远不能变成分钟数（§7.3）。

**删除与级联。**删除情节 → 所有引用它的条目重算支持数；`agent_proposed` 条目支持低于 `PROPOSE_SUPPORT` 时回到 `proposed` 且不再生效；`user_stated` 条目不依赖情节，只随用户删除消失。"本次不保存"的会话不写情节。

**存储。**条目与情节按 owner 存入现有 `documents` 表，读写均经会话 cookie 限定；跨会话记忆因此以 cookie 为用户身份（演示足够，正式登录见 §13）。

**不做的事**：不引入向量记忆库（除非 H2′ 对照证明结构化召回不够）；不从点击、停留时长等隐式行为学习，唯一的隐式信号是 `就这个`；不从条目反推敏感属性（§9）；D3 仍然不是与用户历史的距离——"这个用户喜欢冷门"是 `dimension` 条目，候选的 `obviousness` 仍与用户无关。

---

## 8. 评测计划

v1 的三层方案中，"新颖 vs 合身权衡曲线"随 §1.1 推论 1 一并删除。v2 为两层：一层管"不能是假的"，一层管"懂不懂你"。

### 8.1 第一层：硬闸门（已实现，作为回归保留）

`evals/scenarios.json` 的 20 开发 + 10 保留场景与 `scripts/run_eval.py` 原封不动保留。期望值独立于生产 Validator。当前 30/30 通过。

指标：**错误通过率**（标为 verified 却经独立核对发现硬冲突或证据不足的比例）、**有效方案成功率**（已知有解的案例里返回证据充分且满足全部硬约束方案的比例）。

**已知局限**：现有场景只检查"是否返回了一个不违反硬约束的方案"，无法判断"本来存在更好的解但搜索没找到"。需补一个**暴力参考规划器**（对 fixture 穷举所有集合 × 所有顺序，不受组合数与调用预算限制）产出 ground truth，从而量化可行解召回率与最优性差距。这是修改搜索逻辑前的前置工作，否则改动收益测不出来。

### 8.2 第二层：模拟用户收敛（新增，v2 核心）

构造 8–12 个 persona，每个有一个**对 agent 隐藏的**口味向量与确定性拒绝策略。Agent 与之多轮交互，观察收敛速度。

指标：

- **收敛轮数**：几次 reroll 后被接受（主指标）
- **Accept@k**：k 轮内命中率，k = 1/3/5
- **未修改约束保留率**：学到新偏好时是否毁掉之前已满足的条件
- **错误记忆率**：写入 Profile 的内容有无 persona 依据
- **连续建议差异度**：防止靠反复给同类东西刷收敛
- **成本**：工具请求数、Token、延迟

对照组：

| 组 | 说明 |
|---|---|
| 固定策略 | 按先验相关度排序，被拒取下一名，不解读理由 |
| 无记忆 | 每轮独立，不携带历史 |
| 只用最后一次反馈 | 单步记忆 |
| 完整历史 | 全部对话塞进上下文 |
| **Profile + 相关 Episode** | 本项目方案 |

这套对照同时回答 H1（固定 vs Agent）和 H2（记忆结构消融），共用同一 harness。

**v2.2 新增：跨会话层。**会话内收敛只能证明"会换"，证明不了"记得"。同一 persona 依次进行 S 次会话（初值 6），每次的出发地、星期、时段与窗口按固定种子从 persona 的生活模式中抽取（如 4 次工作日午休 + 2 次周末），会话之间只保留 Profile 与情节。

| 指标 | 定义 | 地位 |
|---|---|---|
| **第 N 次会话首轮接受率** | 按会话序号画 Accept@1 曲线 | **记忆主指标** |
| 情境外误用率 | 一条情境条目在其情境之外改变了排名的轮次占比 | 必报；证明情境条件不是摆设 |
| 提议精确率 | persona 接受的提议 / 全部提议 | 必报 |
| 错误记忆率 | 在"全部同意"persona 上，active 条目与隐藏真值不符的比例 | 必报；普通 persona 会拒绝错误提议，掩盖问题 |
| 记忆召回率 | 隐藏真值中结构化部分（维度、类别、地点、情境规则）被 active 条目覆盖的比例 | 必报；`note` 不计 |
| 漂移恢复会话数 | persona 在第 k 次会话改变一项偏好后，到条目被收窄或退役的会话数 | 仅漂移 persona |
| 召回 Token | 每轮注入的记忆 Token | 成本 |

跨会话对照组：

| 组 | 说明 |
|---|---|
| 无长期记忆 | 每次会话从零开始，只有会话内信念 |
| 扁平 Profile | v2 原设计：仅三个维度计数器，无情境 |
| 类型化、无情境 | §7.4 条目，但 `context` 全部视为全局 |
| 完整情节上下文 | 全部历史情节塞进提示，无结构化条目 |
| **类型化 + 情境** | 本项目方案 |

"扁平 Profile"与"类型化、无情境"两组分别隔离类型化与情境条件各自的贡献；若"类型化 + 情境"不优于前者，按 §8.5 如实报告并回退。

### 8.3 Persona 与模拟器设计

每个 persona 定义：口味向量（室内外、能量、安静/热闹、社交度、花费意愿、可接受距离）、拒绝策略（阈值 + 对应理由 chip）、接受阈值、初始一句话。

要求：

- 拒绝策略必须**确定性**，否则收敛轮数不可复现。
- Agent 不能读取 persona 定义，只能通过 `Feedback` 观察。
- persona 按 D1、D2、类别好恶与情境条件分层；D3 不进入隐藏向量。大众/冷门只作为产品中的一次性局部反馈单独做行为测试。
- **前置检查**：persona 只能使用 `scripts/check_dimensions.py` 在相应池上判定 active 且过闸的维度；不再要求必须凑足三个连续维度。
- 合成 persona 显式标注，不包装成真实用户数据。

**v2.2 修订 persona 设计。**隐藏定义扩展为：维度向量 + 类别好恶 + 0–2 条情境规则（如"工作日午休只要步行 15 分钟内"）+ 地点黑名单 + 生活模式（会话情境分布）。全部为结构化字段，以保证跨会话实验确定可复现。persona 对记忆提议也按确定性规则应答：提议与隐藏真值（含情境）一致则接受，否则拒绝。

分层由 10 个调整为 12 个：

| 层 | 数量 | 考察 |
|---|---:|---|
| 单维度敏感 | 3 | D1、D2 两端取有代表性的三组（受闸门前置约束） |
| 双维度冲突 | 2 | 由 3 减为 2 |
| 类别好恶 | 1 | 维度中性、类别强偏好（如讨厌博物馆但喜欢美术馆） |
| **情境条件** | 2 | 同一维度在两种情境下极性相反；只在一种情境下有偏好 |
| 表述与真实偏好不一致 | 1 | 由 2 减为 1 |
| 口味漂移 | 1 | 第 k 次会话起改变一项偏好 |
| **全部同意** | 1 | 接受一切提议，用于测错误记忆率 |
| 无解或需澄清 | 1 | 不变 |

维度层 persona 仍须等 D2 转绿；类别、情境（用 D1 构造时）、全部同意三层不依赖 D2，可先行开发 harness。

**保留集**：12 个中的 5 个不用于开发调优，至少含 1 个情境条件 persona（v2 原为 10 个中的 4 个）。若查看失败后修复，记录测试暴露并转入回归，下一轮泛化评测使用新 persona。

模型方案至少重复三次，报告逐 persona 成功次数与波动；重复运行不算新的独立场景。

### 8.4 补充：人工相关性标注

固定 0–3 等级准则，对任务简报的合身度与"为什么是现在"的证据有效率打分。作为补充指标，不作主指标。条件允许时第二位标注者复核，否则说明单人标注偏差。

**契机证据有效率**：关于"为什么此时值得去"的可核实陈述中，有正确日期、地点与来源支持的比例。不代表主观吸引力。

### 8.5 初始验收门槛

以下为工程目标，不是现有成绩：

- 固定安全与边界回归中不得出现未处理异常、跨会话记忆泄漏、静默放宽硬约束或隐藏预算超支。
- 保留集所有 verified 输出需通过独立核对；发现失败必须保留记录并修复，不删除难例。
- 硬闸门层：有解案例有效方案成功率至少 80%；conditional 明确报告，不计为成功。
- 收敛层：Profile+Episode 组在保留 persona 上的 **Accept@3 显著高于无记忆组**，且错误记忆率有明确上报。
- **跨会话层（v2.2）**：在保留 persona 上，"类型化 + 情境"组第 3 次及以后会话的首轮接受率高于"扁平 Profile"组；情境外误用率不高于"类型化、无情境"组；提议精确率与错误记忆率如实上报。不满足时采用更简单的记忆方案并保留实验结论。
- Agent 相对固定策略无收益时，采用固定策略并保留实验结论。

报告原始分子/分母、逐案例结果与失败类型。不得预填"提升 30%"等结果。

---

## 9. 可靠性、安全与可观测性

沿用 v1 §9，全部保留：

- Provider Adapter 隔离原始 Schema，统一错误类型与获取时间。
- 可重试错误有限退避；缓存 TTL 按数据类型配置，保留原 fetched_at；证据过期不显示为新鲜。
- 工具层限制来源、参数与网络目标；阻止本地/私网地址访问；API Key 不进入模型上下文、前端或日志。
- 网页指令作为不可信内容处理，含诱导改预算、调用任意 URL、泄漏密钥的提示注入测试。
- 请求版本与检查点；旧请求的迟到结果不能覆盖新要求。
- 请求可取消，重复提交幂等；公开演示增加限流与花费上限。
- Trace 记录 run_id、请求版本、节点、动作摘要、工具耗时、缓存、证据引用、失败类型、Token 和成本。

v2 新增，针对口味推断：

- **推断不写入 Profile**。只有经用户确认的提议才成为长期记忆；推断结果随会话结束失效。
- **口味维度不得推断敏感属性**。不从偏好反推健康状况、宗教、性取向或经济状况；维度集合固定且可审计。
- **Trace 必须记录本次押注了哪个维度、依据是什么**，否则"懂用户"不可解释也不可评测。

v2.2 新增，针对长期记忆：

- **跨用户隔离**：条目与情节的每次读写都经 owner 限定，含一项"另一会话读不到"的测试。
- **删除即生效**：删除后的下一轮召回、打分与叙述都不得出现该条目；派生条目按 §7.4 级联重算。
- **笔记是不可信文本**：`note` 与 `preference` 同等对待，只作数据，不得改变工具调用、预算或约束（沿用提示注入测试）。
- **不记录精确位置**：情节只存情境桶与候选 id，不存出发坐标。

---

## 10. 里程碑

### 已完成

| 阶段 | 状态 | 交付物 |
|---|---|---|
| M0 数据与契约探针 | ✅ | `docs/data-feasibility.md`、`data/probes/20260914T041841Z`（v2.4 删除其余探针脚本与快照） |
| M1 确定性多站基础与 Trace | ✅ | `models.py` 全量模型、`planner.validate()` 全链条验证、`ReplayTools` + 出发时刻敏感缓存、`Trace`、`storage.py` CAS 写入 |
| M2 Replay、固定策略与首批 Eval | 🟡 | 30 场景 + 独立期望 + `reports/latest-eval.json` + 开发视图。缺"强基线对照"——目前只有固定策略一条路径，无对照对象，随 M5′ 补齐 |
| M4 产品外壳（部分） | 🟡 | SSE、锁定/删除/替换、取消、幂等、会话隔离已实现；交互形态待按 §2.2 重做 |

验证现状的历史记录（2026-09-14，v2.1 改动后）：`ruff` 通过，`pytest` 94 项通过，eval dev 20/20 + holdout 10/10，前端构建通过。维度状态已由 v2.3 的 D1、D2 生产 / D3、D4 预留决定取代。

### 重排后的剩余阶段

| 阶段 | 估算 | 交付物与通过条件 |
|---|---:|---|
| ~~M3′-0 候选池扩展~~ | — | **v2.1 取消为主路径**：不再逐个人工核验 19 个候选进 allowlist。shortlist 保留为 Replay 评测基准的可选素材 |
| **M3′-1 判定范围收窄** | ✅ | `Request.venue_facts`、`Check.advisory`、`Itinerary.advisories`；agent 提示词带完整日期；叙述不得把未核实项写成事实；3 项 planner 测试 + 1 项 agent 测试（§6.1） |
| **M3′-2 实时地点发现**（新前置） | 3–4 日 | 🟡 **代码完成，待全量快照**。已完成：`places.py`（索引、半径检索、分类配额、营业时间软排除、D3 推导）、`Request.catalog="osm"` 接入 Replay/Live 两种路线来源、agent `search_places`（只选类型，结果为空时不收窄）、CLI `--catalog osm --at LAT,LON`、`check_dimensions.py --places`、日常类别与动作提示、受控随机抽样（附带修复：被换掉的地点此前可能作为顺路站回到行程，现作为 `excluded_ids` 传入规划，`Request.excluded_ids` 上限相应放宽到 64）、共 16 项新测试。用 M0 探针的 3 个区域（157 个地点）试建索引：D1 33%、D3 31% 可分，D2 少数侧 11% 仍不足——探针区域太小，需全悉尼快照复测。待做：运行 `fetch_places.py` 取全量快照。原计划：选定数据源（§13）；实现 `retrieve` 适配器（固定端点、Key 走 Header、类型化错误、预算）；`search_places` 搜索意图 → 落地为真实候选；类别默认停留时长；实时候选的 D3 计算；检索快照落盘；在快照上重跑 `check_dimensions.py`，D2 转绿 |
| **M3′ 口味模型与反馈闭环** | 3 日 | ~~`Feedback`/`TasteState` 模型~~（已完成）；reroll + 三类作用域 chip（§2.2）接入 API 与前端；三层状态与作用域区分；排序函数按 §4.4 重写；会话内差异度可计算。（`obviousness` 字段与 13+19 个候选的标注已完成） |
| **M3′-M 类型化记忆**（v2.2 新增） | ✅ | **已完成**：`memory.py` + `taste.py` 证据规则 + agent 接入（召回、硬跳过、打分上限、Trace 与 `Quest.memory_ids`），pytest 总数 110 → 145，5 处规则变异均被测试拦下；`not_this_kind`、`never_here` 两个 reason 与接受事件（`TasteState.accept`）已在模型层落地。原计划：`Context` 推导、`Episode`、`MemoryItem` 四类条目；合并与提议阈值；冲突优先级；例外、漂移与 stale；召回与打分上限；删除级联；owner 隔离。全部为确定性代码 + 单元测试，不依赖模型与 D2。**通过条件**：§7.4 每条规则至少一项测试，含"同会话三次不足以提议""不同情境的反例不算反例""删除后下一轮不受影响""记忆加分不压过一个维度命中" |
| **M3′-M API 与抽屉**（v2.2 新增） | ✅ | **已完成**：reroll（11 个理由）、接受、提议确认（服务端按 signature 重算，客户端无法伪造）、笔记、删除条目与经历、暂停记录；固定策略路径用 `agent.rank_itineraries` 按口味给可行方案排序（无模型调用），无可行方案命中时 Trace 写 `taste_miss` 并在界面提示；前端 chip 分组、提议卡片、偏好抽屉。pytest 152 项。原计划：接受动作与新 chip 接入 API；偏好抽屉按条目展示情境、来源与支持情节；提议卡片；"本次不保存" |
| **M4′ Agent 闭环** | 4 日 | 🟡 **基本完成**（2026-09-14）：API 与 SSE 接入（`?strategy=agent`，逐步推送进度）、超时 / 模型错误 / 押注全部验证失败三种降级到固定策略并在界面说明、reroll 复用父意图、「其他 + 一句话」由 `interpret_feedback` 映射（不能映射成永久拉黑；仅未映射的文本可提议笔记）、叙述中无记忆依据的「你说过/你喜欢」由执行器删除（真实模型实测发现）。**未做**：`inspect_candidate` 补查——目前没有第二个数据源可查，动作空间为空，待商业地点数据源或官方页面抓取接入后再做。此前：已完成：模型适配器（`llm.py`，OpenAI，无新依赖）、`infer_intent`、`choose_probe`、目标向量与候选打分、locked_ids 交棒执行器、`narrate` 与证据引用校验、hook 的"此刻"素材（`moment.py`：日落、时间窗口、路线、同向记忆，均可引用，§6.1）、CLI（`scripts/run_agent.py`）、11 项假模型测试。待做：接入 API 与 SSE、补查决策（`inspect_candidate`）、记忆写入提议接入前端、超时与降级路径 |
| **M5′ 模拟用户评测** | 3 日 → **5 日**（v2.2） | 🟡 **harness 完成，正式实验未开工**（2026-09-14）。已完成：`reference.py` 暴力参考规划器 + `scripts/check_reference.py`（30 场景：有可行解 27 个，固定策略命中 27/27、verified 25/25，3 个场景返回站数少于可行上限，无判定分歧）；`personas.py` 12 个 persona（5 个保留集，p02/p05 因 D2 未启用标为 blocked）、确定性判官与记忆真值；`harness.py` 六组对照（fixed / no_memory / last_feedback / flat_profile / typed_no_context / typed_context，消融只改组能保存的内容、不改产品代码）、`RuleModel` 规则替身、跨会话指标（按会话序号首轮接受率、记忆塑造的首轮占比、情境外误用、提议精确率、全同意 persona 错误记忆率、记忆召回）；`scripts/run_personas.py`；10 项测试。**首次开发运行**（M0 探针小索引 157 个地点、7 个开发 persona、38 个会话、RuleModel，非冻结实验）：首轮接受率 fixed 0.24 → no_memory 0.34 → typed_context 0.42，但 flat_profile / typed_no_context 为 0.45，第 3 次及以后会话首轮 0.46 对 0.50；typed_context 提议 6 次全部被接受（精确率 1.0），扁平组 8–9 次约一半被拒；记忆召回 5/10。**解读受限**：样本太小，且与 §13 第 10 条预判一致——情境桶偏细，6 次会话分到 3 种情境后很少攒够提议阈值，情境条件的收益被阈值吃掉。**大池子复测**（未抓完的全量快照，D1+D2 启用，6 个开发 persona、38 个会话，RuleModel）：首轮接受率 **fixed 32/38 高于所有 agent 组**（no_memory 20/38，flat/typed_no_context 23/38，typed_context 22/38）；记忆组中提议全部被接受（3/3），记忆召回 3/9，全同意 persona 无错误记忆。**归因（同日追查后修正）**：先按"agent 不看距离"加了时间先验（窗口 ≤120 分钟且无反馈/记忆时 D2 默认近，`agent.NEAR_BY_DEFAULT_MINUTES`），在同一索引上做有/无先验对照，首轮接受只变化 0–1 个会话，**距离不是主因**。逐 persona 拆解后：密集城区里离起点最近的往往是奶茶店、咖啡馆这类日常地点（D1 = 室内），而 6 个开发 persona 中 4 个偏室内（p01、p07 午休、p10 漂移后、p11），固定策略因此"碰巧"命中；agent 在每个会话开头对 D1 抛硬币试探，一半押到户外。结论：**这是开发 persona 偏室内 × 城区最近点为室内 × RuleModel 盲目试探三者叠加的产物，不能当作 H1 的证据**。需要：persona 集在 D1 两端平衡、真实模型复测、以及记忆阈值让偏好更早形成（typed_context 在 38 个会话中只有 2 个首轮受记忆影响）。时间先验本身与计划一致，保留。

**历史实验（已由 v2.3 取代）**：D3 曾用 Wikidata sitelinks 改成冷门 / 有点名气 / 大众三档，并进入 persona。最新完整索引上少数侧实际只有 10.9%，且全局“小众偏好”的产品假设本身不成立。因此以下 D3 persona 数字只作为历史实验记录，不再代表当前方案：

| 组 | 模型 | 首轮接受 | 3 轮内接受 | 第 3 次起首轮 | 提议被接受 | 记忆召回 |
|---|---|---:|---:|---:|---:|---:|
| fixed | — | 25/44 | 29/44 | 16/30 | — | 0/11 |
| no_memory | RuleModel | 20/44 | 41/44 | 14/30 | — | 0/11 |
| typed_context | RuleModel | 23/44 | 41/44 | 17/30 | 6/6 | 4/11 |
| no_memory | gpt-4o-mini | 22/44 | 41/44 | 14/30 | — | 0/11 |
| typed_context | gpt-4o-mini | 22/44 | 40/44 | 14/30 | 8/8 | 5/11 |

读法：固定策略第一枪还行，但被拒后几乎不会收敛（3 轮内 29/44）；反馈驱动的 agent 3 轮内 40–41/44。**跨会话首轮上，真实模型下记忆组与无记忆组持平（14/30）**，是本轮尚未解决的问题。

**真实模型暴露并修掉的产品 bug**：`search_places` 的提示不含本会话反馈，gpt-4o-mini 在 90 分钟午休时只选日常类别（全是室内），类型过滤把户外地点全部删掉——偏户外的 persona 连说五次"想动一动"仍拿到五家奶茶店（修复前 3 轮内接受 24–26/44）。修复：提示里带上用户说过或确认过的偏好；执行器在模型的类型过滤删光了符合这些偏好的候选时补回（`agent.keep_what_the_user_asked_for`，Trace 记 `restore_feedback`）。首版只保护本会话反馈，之后扩展到确认过的记忆——扩展后尚未用真实模型复测，这可能正是上表跨会话首轮持平的原因。

**阈值扫描**（RuleModel，仅开发 persona）：`PROPOSE_SUPPORT` 3 → 2 时 typed_context 记忆召回 4/11 → 7/11、提议精确 6/6 → 11/12、首轮与第 3 次起各 +1；flat_profile 在 2 时精确率跌到 8/17、全同意 persona 错记 1/2。**情境桶粒度无法用现有 persona 测**：每个 persona 同一时段的窗口长度固定，"时段×窗口"与"只按时段"结果完全相同，需要同一时段不同窗口长度的生活模式。阈值暂未改动。

**v2.3 情境泛化修正**（2026-09-15，完整 OSM 索引，D1+D2 启用，**仅 7 个开发 persona、44 个会话**，保留集未参与分析与调参）。v2.3 首轮真实模型运行中，typed_context 首轮接受 27/44，低于 flat_profile / typed_no_context 的 34/44，两次重复结果相同；差距全部来自三个整体偏户外的 persona（p01、p04、p10）。**归因**：合并规则要求每个情境单独达标（同情境 ≥3.0 支持、≥2 会话），而 persona 在 3 种情境间轮换，每种只出现两次——p01 的全局条目第 5 次会话才出现，扁平组第 2 次会话就有。RuleModel 会读取提示里的观测直接利用，没有记忆也能首轮命中，因此掩盖了这个延迟（RuleModel 下三组持平于 26/44）。**修正**见 §7.4：跨情境合计路径 + reroll 证据要求。第一版不加 reroll 要求时，RuleModel 下召回升到 8/11，但提议精确率从 1.0 跌到 8/11——三次被拒的提议全是给无距离偏好 persona 的全局"就近"，来源是短窗口就近默认值带来的接受证据。

| 组（gpt-4o-mini，开发 persona） | 首轮接受 | 第 3 次起首轮 | 记忆塑造首轮 | 提议被接受 | 记忆召回 | 错误条目 |
|---|---:|---:|---:|---:|---:|---:|
| no_memory | 22/44 | 9/23 | 0/44 | — | 0/11 | — |
| flat_profile | 34/44 | 19/23 | 13/44 | 6/12 | 6/11 | 0/6 |
| typed_no_context | 34/44 | 19/23 | 13/44 | 6/12 | 6/11 | 0/6 |
| typed_context（修正前，重复 2 次相同） | 27/44 | 14/23 | 5/44 | 8/8 | 5/11 | 0/5 |
| **typed_context（修正后）** | **34/44** | **19/23** | **12/44** | **5/5** | 5/11 | 0/5 |

读法：typed_context 在首轮上追平扁平组，同时保住情境条件的两项收益——提议全部被接受（扁平组一半被拒），p07"午休想坐、周末想走"首轮 4/6 高于扁平组 3/6；情境外误用 0/1。唯一落后的是 p02（只靠接受体现的就近偏好学不到，4/6 对 5/6）。未受改动影响的三组与修正前两次运行逐项一致，差异可归因于代码而非模型波动。修正后真实模型只跑了 1 次。RuleModel 下 typed_context 首轮 26/44 → 28/44、召回 4/11 → 6/11、精确率保持 1.0。报告：`reports/latest-personas-openai.json`（= `personas-openai-v23-pooled-r1.json`），RuleModel 为 `reports/latest-personas.json`。

**待做**：全量 OSM 快照上重跑；阈值扫描（§13 第 8 条，只在开发 persona 上）；真实模型重复 3 次；D2 转绿后补 p02/p05；"完整情节上下文"对照需要 LLM，RuleModel 下未运行。原计划：persona 集与模拟器；收敛实验；五组对照；暴力参考规划器与可行解召回率；逐 persona 结果与波动。**v2.2 增加**：12 个 persona 的扩展隐藏定义与确定性提议应答；跨会话 harness（S 次会话、固定情境抽样）；跨会话五组对照；按会话序号的首轮接受率曲线 |
| **M6′ 产品形态重做** | 2 日 | 一句话输入 → 单任务卡 → reroll；表单降级为展开调整；故障注入移入开发视图；任务简报渲染与证据引用校验 |
| **M7′ 冻结评测、部署与展示** | 2–3 日 | 两层指标全量结果、Bad Case 分析、Docker 部署、3 分钟演示 |
| M8 MCP 加分项 | 0–2 日 | 暴露少量只读工具并由另一真实客户端调用；合约与错误测试 |

**顺序依据**：M3′-2 先于一切，因为口味维度不足会让 M5′ 的核心实验退化（§4.3.1），而 v2.1 下地理跨度由实时检索提供；M5′ 的 persona 实验跑在检索快照上，不跑实时请求；M3′ 先于 M4′，因为 agent 需要有 `TasteState` 可读写才有决策对象；M5′ 的暴力参考规划器应在任何搜索逻辑改动之前落地。

**v2.2 顺序补充**：M3′-M 类型化记忆是纯确定性代码，与 M3′-2 全量快照、D2 闸门互不阻塞，可立即并行开工；它先于 M4′ 接入 API，因为 agent 的召回与提议需要条目模型才有读写对象。跨会话 harness 可先用类别、情境（D1 构造）、全部同意三层 persona 调通，维度层 persona 等 D2。

时间不足时的削减顺序：MCP → 检索消融 → 局部修复（H3）→ **笔记类条目 → stale 衰减**（v2.2）→ 额外 Provider → 页面装饰。**保留**：reroll 闭环、模拟用户收敛实验、**跨会话记忆实验与情境条件**（v2.2）、全链条验证、Trace、固定策略对照、Replay。

---

## 11. 演示与求职证据

三分钟演示脚本：

1. 输入"下午空出来了，不想待在家"和一段时间，得到一个支线任务。展示系统推断了什么（午休还是下班后、可走多远），以及为什么是现在。
2. 点"换一个"，理由选"太安静了"。新任务方向明显不同——展示它换的不是随机项，而是针对该维度。
3. 再换一次，系统提议"要不要记住你偏好热闹一些的地方"，用户确认。
3′.（v2.2）**跨会话**：同一用户"第二天午休"再来，首轮直接给近处的小事；"周六下午"再来，同一个人拿到远一些的观景点——同一维度在两种情境下方向相反。打开偏好抽屉，看到条目写着"工作日午休 · 不想走远 · 依据 3 次经历"，删掉它，下一轮立刻不再受影响。
4. 开发视图：本轮押注了哪个维度、依据是什么、调用了哪些工具、哪些组合因时间冲突被淘汰、证据来源与有效期。
5. 展示模拟用户收敛曲线、**按会话序号的首轮接受率曲线（五组对照）**与记忆消融对照，以及一条实际失败案例；另保留 Live 查询片段证明真实接入。

仓库交付清单：

- `README.md`：产品、架构、运行方式、边界、结果与 Demo。
- `docs/data-feasibility.md`：真实数据探针与许可。
- `docs/architecture.md`：状态、动作、工具契约与设计取舍。
- `evals/`：硬约束场景、persona 集、快照或重建方法、基线与评分。
- `reports/`：配置、指标、逐案例与逐 persona 结果、Bad Case、对照结论。
- `backend/tests/`：时间边界、Provider 合约、工具权限、状态恢复、记忆隔离。
- CI：格式检查、必要测试、确定性回归；付费模型评测显式触发并记录版本。
- 可运行部署、截图或视频；未验证的高并发、生产可用性、训练和多 Agent 能力不写入简历。

---

## 12. JD 对应与范围取舍

| 目标要求 | 项目证据 | 相对 v1 变化 |
|---|---|---|
| Agent Planning、Tool Use、工作流 | 探索/利用决策、动作契约、固定策略对照 | 研究问题更贴近 agent 本身 |
| Context、Memory、Runtime | 反馈驱动的三层记忆、持久状态、恢复与异步任务；**v2.2**：类型化 + 情境条件记忆，合并、冲突、漂移、召回、删除级联的完整管理流程，跨会话实验量化 | **显著增强**：从 CRUD 变为可评测的学习闭环；v2.2 起为项目主亮点 |
| 自动评测、Trace、Bad Case | 两层评测、模拟用户 harness、回放、故障注入、归因 | **显著增强**：模拟用户收敛是 agent 评测的标准技术 |
| RAG、混合检索、效果分析 | 真实语料与检索消融 | 不变，仍后置 |
| MCP、工具生态 | 同一工具供独立客户端调用，有契约测试 | 不变 |
| 全栈、流式交互、服务工程 | Python + TypeScript、SSE、部署、取消、限流 | 不变 |

**这次转向的取舍**：M1/M2 的证据与验证体系是目前工程完成度最高的部分，v2 让它从"主角"降为"地基"。它在简历与演示中的位置从第一句话挪到第三句话。判断依据是"我实现了一个多站行程验证器"与"我做了一个会从拒绝中学习你口味的 agent，并用模拟用户量化了它学得多快"相比，后者更贴合目标岗位要求的 Agent / Memory / 评测能力。

不为覆盖关键词加入多 Agent、SFT/RL、知识图谱、端侧推理、高并发微服务。

## 13. 待定选择

1. ~~模型与预算~~ —— **已定**：OpenAI，通过 `llm.py` 适配器接入（端点、模型、Key 全部走环境变量，换供应商只改配置）。默认 `gpt-4o-mini`，可用 `OPENAI_MODEL` 覆盖。评测时须记录确切模型与版本。
2. **Persona 数量与分层**：10 个（分层见 §8.3），模拟器跑通后按收敛轮数方差调整。
3. ~~口味维度集合~~ —— **已冻结，见 §4.3**：D1 形态、D2 移动意愿启用；D3 常规度、D4 氛围预留。D3 只服务一次性同类 reroll。
4. ~~候选池扩展~~ —— **v2.1 改为实时发现**（§5）。描述质量仍是瓶颈。**v2.2 补充**：hook 的"为什么是现在"改由确定性的"此刻"事实提供（日落、时间窗口、路线、记忆，§6.1），不再从百科简介或评论摘要提取；地点当期内容作为 RAG 切入点后置（§7.1）。叙述引用须能对上真实数据。
5. **公开演示方式**：本地视频或受限在线 Demo；上线前决定限流与隐私配置。
6. ~~实时地点数据源~~ —— **已定：OSM**（Overpass 批处理快照 + 本地 SQLite 索引，§5）。营业时间或热度不够用时，再按需叠加商业数据源。
7. **商业数据源的营业时间能否回到判定**：决定 §6.1 第 3 层补丁的范围，即产品输出主要是 verified 还是带提示的 verified。
8. **（v2.2）记忆阈值**：§7.4 常量的初值是拍定的，跑通跨会话 harness 后按提议精确率与首轮接受率的权衡校准，校准后冻结再跑保留集。
9. **（v2.2）用户身份**：跨会话记忆目前以会话 cookie 为身份，清 cookie 即失忆。演示足够；公开 Demo 是否需要轻量登录，与第 5 项一并决定。
10. **（v2.2）情境桶粒度**：`slot × window` 共 12 个桶，桶越细情境条目越难攒够支持。若跨会话实验中大多数条目停在 proposed 之前，先合并 `window` 再考虑其他调整。**v2.3 进展**：观察到的问题不在 `window`，而在"全局条目要求每个情境单独达标"；已改为允许跨情境合计（§7.4），桶粒度暂不调整。

下一项实际工作不再是凑出第三个全局维度；应在完整 OSM 索引上验证 D1、D2、类别偏好、情境记忆和一次性局部 reroll 的行为。
